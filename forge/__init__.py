"""forge v2 — a sans-IO coding agent: pure kernel over one event log.

Layers: kernel (pure values + step) < ports/policy < drive/adapters/testing
< front. The import law is enforced by .importlinter, not prose.
"""

from __future__ import annotations
