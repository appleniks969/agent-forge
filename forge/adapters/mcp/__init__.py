"""adapters.mcp: the MCP manager that manufactures Tools.

Import names from forge.adapters.mcp.manager; one import path per name,
no re-export aliases. Imports without the optional mcp SDK installed —
the SDK loads lazily inside connect().
"""

from __future__ import annotations
