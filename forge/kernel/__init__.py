"""kernel: pure, synchronous, stdlib-only — zero internal deps.

Import names from their defining module (forge.kernel.types, .events,
.state, .step); one import path per name, no re-export aliases.
"""

from __future__ import annotations
