// Placeholder entry point.
// The agent should keep this file (or replace it) and ensure `MainKt` exists
// with a `main()` function so `./gradlew run` works.
fun main() {
    println("notes-app placeholder — agent must implement the app per TASK.md")
}
